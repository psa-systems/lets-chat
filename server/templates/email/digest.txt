You have unread activity in Let's Chat. Here is a summary of what you missed while you were away.

{% if !dm_sections.is_empty() -%}
=== Direct messages ===

{% for section in dm_sections -%}
From {{ section.peer_username }}
{% if !server_url.is_empty() -%}
  ({{ server_url }}/dm/{{ section.peer_id }})
{% endif -%}
{% for item in section.items -%}
  {{ item.author }} at {{ item.created_at }}:
    {{ item.snippet_plain }}
{% if !server_url.is_empty() -%}
    -> {{ item.deep_link }}
{% endif -%}
{% endfor %}
{% endfor -%}
{% endif -%}

{% if !room_sections.is_empty() -%}
=== Mentions ===

{% for section in room_sections -%}
#{{ section.room_name }}
{% if !server_url.is_empty() -%}
  ({{ server_url }}/room/{{ section.room_id }})
{% endif -%}
{% for item in section.items -%}
  {{ item.author }} at {{ item.created_at }}:
    {{ item.snippet_plain }}
{% if !server_url.is_empty() -%}
    -> {{ item.deep_link }}
{% endif -%}
{% endfor %}
{% endfor -%}
{% endif -%}

{% if overflow_count > 0 -%}
...and {{ overflow_count }} more.{% if !server_url.is_empty() %} Open {{ server_url }}/ to see them.{% else %} Open Let's Chat to see them.{% endif %}

{% endif -%}
---
You are receiving this because email digests are enabled for your account.
{% if !server_url.is_empty() -%}
Change your notification preferences at {{ server_url }}/settings.
{% else -%}
Change your notification preferences in the Let's Chat settings page.
{% endif -%}
